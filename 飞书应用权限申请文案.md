# 飞书应用权限申请文案

## 一、业务场景描述和必要性说明

本应用（尔将AI助手）服务于教育培训机构，用于自动化学情分析、续费风险预警、批量报告生成等教学管理场景。

**业务场景：**
- 培训机构老师通过飞书群聊发送学生的上课数据（Excel），机器人自动进行风险分析，识别出续费风险高的学生，并返回分析结果。
- 机构管理者在飞书群内通过斜杠命令（/risk /report /help）即可触发数据分析，无需登录后台系统，极大提升工作效率。

**必要性：**
- 目前分析系统只能在 Web 页面使用，老师需要频繁切换页面，操作成本高。
- 飞书是机构日常沟通的核心工具，将分析能力嵌入飞书，让老师**在聊天中就能完成数据分析**，无需离开飞书，大幅降低使用门槛。
- 同时支持文件上传和消息推送，实现"发文件→自动分析→返回结果"的自动化闭环。

---

## 二、功能描述

1. **接收消息事件**（im:message）
   - 接收用户在群聊中 @机器人 发送的文本命令（如 /risk、/report、/help）
   - 接收用户发送的 Excel 数据文件，触发自动分析

2. **发送消息**（im:message）
   - 将分析结果以文本消息形式回复到群聊
   - 将分析报告以文件形式发送到群聊

3. **下载文件资源**（im:resource）
   - 下载用户发送的 Excel 文件到本地进行数据分析

4. **群聊管理**（im:chat）
   - 创建测试群聊
   - 将机器人加入群聊

---

## 三、权限申请理由

### 消息与群组 API 权限

| 权限 | 用途 |
|------|------|
| `im:message` | 接收用户消息命令、发送分析结果文本和文件到群聊 |
| `im:resource` | 下载用户上传的 Excel 数据文件，用于后续分析处理 |
| `im:chat` | 创建测试群，管理机器人所在群聊 |

**说明：** 以上权限仅用于实现"自动接收数据→分析→返回结果"的核心功能闭环，不会超范围使用。所有数据仅用于当次分析，不存储用户隐私信息。

---

## 四、应用可用范围有新增的理由

- 初始版本仅限内部测试群使用
- 正式上线后，需要扩展到机构的所有教学群聊，覆盖更多老师和学生
- 让每个有需求的老师都能在群聊中直接使用机器人进行数据分析，无需额外配置